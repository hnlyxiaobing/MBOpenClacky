# Agent TODOs

- [ ] Investigate LZW large-payload decode/encode stability around code-length transitions (see `codec/pdfcodec/pdfcodec_coverage_extra_test.mbt`).
- [ ] Review and either apply or drop the existing git stash: `stash@{0}`.

