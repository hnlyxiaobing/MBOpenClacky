# <功能/Bug 名称> · 增量 Spec

> **创建日期**: YYYY-MM-DD  
> **状态**: 讨论中 / 实施中 / 已完成  
> **关联总览**: `<gap 文档引用>`  
> **关联历史 spec**: <链接或文件名>  
> **来源差距**: <G## - 简述>  
> **依赖**: <前置 spec 或"无">  
> **灰度 key**: <如有>

<!-- 
  v2 规则：标有 [必填] 的章节不可留空，否则 spec 不允许进入 active/。
  "现状分析"必须包含"经代码验证"标记，列出实际执行的 grep/glob 命令和结果。
  新 spec 先放 specs/draft/，通过对抗性审核后移入 specs/active/。
-->

## 问题描述 [必填]

<当前出了什么问题 / 需要增加什么>

## 现状分析 [必填 - 含代码验证]

<!-- 必须包含"经代码验证"标记。对每个"缺失/已有"声称，列出实际执行的验证命令和结果。 -->

### 验证记录

| 声称 | 验证命令 | 结果 | 结论 |
|------|---------|------|------|
| "X 功能缺失" | `grep -r "X" lib/` | 0 命中 | 确认缺失 |
| "Y 文件存在" | `glob "lib/**/Y*"` | 1 结果 | 确认存在 |

### 详细分析

<代码当前状态，引用 codemap 或历史 spec>

## 决策 [必填 - 含为什么]

1. **决策 1**：<为什么这么选>
2. **决策 2**：<为什么这么选>

<!-- MoonBit 约束检查（如有涉及）：
- AOT 约束：是否涉及动态加载 trait？是 -> 不可行，改用 shell 命令
- crescent 路由：是否声称"crescent 不支持 X"？是 -> grep 验证已有用法
- FFI：是否涉及 C 库？是 -> 列出 native-stub 和 link.native 配置
-->

## 改动范围 [必填]

### 涉及文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `<path>` | 新建/修改 | <做什么> |

### 不涉及文件

- <明确不碰的范围>

## 实施计划 [必填]

### 任务包 1：<名称>（预估 X 天）
- <具体步骤>

### 任务包 2：<名称>（预估 X 天）
- <具体步骤>

## 验收标准 [必填]

- [ ] 标准 1
- [ ] 标准 2
- [ ] `moon check` 0 errors（<涉及的包>）
- [ ] `moon test <包>` 通过

## 风险评估 [必填]

| 风险 | 影响 | 缓解方案 |
|------|------|---------|
| <风险 1> | 高/中/低 | <方案> |

## 依赖关系 [必填]

- **前置依赖**：<需要先完成的 spec 或功能，或"无">
- **后置依赖**：<依赖本 spec 的 spec 或功能，或"无"）

## 变更记录 [必填]

| 日期 | 变更内容 | 原因 |
|------|---------|------|
| YYYY-MM-DD | 初始版本 | - |
