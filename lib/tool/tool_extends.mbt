///|
extend Browser with Tool::{
  name,
  description,
  parameters,
  category,
  execute,
  format_call,
  format_result,
  to_function_definition,
}

///|
extend TrashManager with Tool::{
  name,
  description,
  parameters,
  category,
  execute,
  format_call,
  format_result,
  to_function_definition,
}

///|
extend RequestUserFeedback with Tool::{
  name,
  description,
  parameters,
  category,
  execute,
  format_call,
  format_result,
  to_function_definition,
}

///|
extend Write with Tool::{
  name,
  description,
  parameters,
  category,
  execute,
  format_call,
  format_result,
  to_function_definition,
}

///|
extend WebSearch with Tool::{
  name,
  description,
  parameters,
  category,
  execute,
  format_call,
  format_result,
  to_function_definition,
}

///|
extend WebFetch with Tool::{
  name,
  description,
  parameters,
  category,
  execute,
  format_call,
  format_result,
  to_function_definition,
}

///|
extend TodoTool with Tool::{
  name,
  description,
  parameters,
  category,
  execute,
  format_call,
  format_result,
  to_function_definition,
}

///|
extend Terminal with Tool::{
  name,
  description,
  parameters,
  category,
  execute,
  format_call,
  format_result,
  to_function_definition,
}

///|
extend MemoryTool with Tool::{
  name,
  description,
  parameters,
  category,
  execute,
  format_call,
  format_result,
  to_function_definition,
}

///|
extend InvokeSkill with Tool::{
  name,
  description,
  parameters,
  category,
  execute,
  format_call,
  format_result,
  to_function_definition,
}

///|
extend Grep with Tool::{
  name,
  description,
  parameters,
  category,
  execute,
  format_call,
  format_result,
  to_function_definition,
}

///|
extend Glob with Tool::{
  name,
  description,
  parameters,
  category,
  execute,
  format_call,
  format_result,
  to_function_definition,
}

///|
extend FileReader with Tool::{
  name,
  description,
  parameters,
  category,
  execute,
  format_call,
  format_result,
  to_function_definition,
}

///|
extend Edit with Tool::{
  name,
  description,
  parameters,
  category,
  execute,
  format_call,
  format_result,
  to_function_definition,
}
